import mongoose from "mongoose"

const { Schema } = mongoose

export class <%= _nameInPascal %>Model extends Schema {

    constructor()
    {
        super()
        const <%= _name %>Schema = {
            title: {
                type: String,
                required: true
            },
            createdAt: {
                type: Date,
                default: Date.now
            },
            updatedAt: {
                type: Date,
            }
        }

        this.add(<%= _name %>Schema)

    }
}